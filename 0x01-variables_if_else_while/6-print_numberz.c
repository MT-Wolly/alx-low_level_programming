#include <stdio.h>

/**
 * main - Prints the numbers from 0 - 9 using puts.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
