#include "holberton.h"

/**
 * main - tests if function prints the correct output
*if integer is positive, zero or negative
*
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
